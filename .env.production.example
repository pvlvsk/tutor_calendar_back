# ============================================
# PRODUCTION ENVIRONMENT (.env для продакшен сервера)
# ============================================
# Скопируй этот файл как .env на сервере:
#   cp .env.production.example .env
#   nano .env  # Заполни CHANGE_ME реальными данными
#
# После первого запуска выполни:
#   docker exec -it teach-postgres psql -U postgres -c "ALTER USER postgres WITH PASSWORD 'твой_пароль';"
# ============================================

# Node Environment
NODE_ENV=production

# Database (Docker)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=CHANGE_ME
POSTGRES_DB=teach_mini_app

# DATABASE_URL формируется в docker-compose.yml, но можно указать явно
DATABASE_URL=postgresql://postgres:CHANGE_ME@postgres:5432/teach_mini_app

# Backend
BACKEND_PORT=3000

# JWT (используй надёжный секрет минимум 32 символа!)
# Сгенерировать: openssl rand -base64 32
JWT_SECRET=CHANGE_ME_MIN_32_CHARS
JWT_EXPIRES_IN=7d

# Telegram Bot (продакшен бот)
BOT_TOKEN=CHANGE_ME
BOT_USERNAME=your_bot_username

# Beta Access (опционально)
# BETA_CODE=beta_2025

# ============================================
# Admin Panel
# ============================================
# JWT секрет для админ-панели (минимум 32 символа!)
# Сгенерировать: openssl rand -base64 32
ADMIN_JWT_SECRET=CHANGE_ME_MIN_32_CHARS

# Mini App URL (фронтенд)
WEBAPP_URL=https://quickbotics.ru
